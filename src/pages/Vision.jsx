function Vision() {
    return (
        <>
        <h1>Vision</h1>
        <p>The event recommendation API shows different events as concerts, theatre and lectures based on location. We have implemented our own data for now, but in SP3, we may retrieve the data via the Ticketmaster API.</p>
        </>
      );
}

export default Vision;